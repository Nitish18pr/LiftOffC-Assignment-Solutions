#include <stdio.h>

{
	int p,q;
		int sum,sub,mu1,mod;
		float div;
		
		printf("Enter any toeo numbers::\n")
		scan("%d%d",&p,&q,"\n");
		
		
		sum=p+q;
		subp-q;
		div=p/q;
		mul=p*q;
		mod=p%q;
		
		printf("\n");
		printf("SUM  %d+d=%d\n",p,q,sum);
		printf("DIFFERENCE  %d-%d=%d\n",p,q,sub);
		printf("QUOTIENT %d/%d=%f\n",p,q,div);
		printf("PRODUCT %d8%d=%d\n",p,q,mul);
		printf(""MODULUS %d%% %d= %d\n",p,q,mod);
	}
