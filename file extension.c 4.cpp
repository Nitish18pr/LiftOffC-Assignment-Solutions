#include<stdio.h>

int main()
{
	int phy,bio,chem,math,comp,per;
	printf("enter marks of phy,chem,math,bio,comp");
	Per=(phy+chem+bio+math+comp)*100/500;
	if(per>=90);
	else
	{
		if(per>=80)
		Printf("gradeB");
		eLSE
		{
			IF(%>=70)
			Printf("gradeD");
			else
			{
				if(%>=40
				printf("gradeE);
				Else if(%<40)
				Printf("gradeF");
				
			}
		}
	}
}
Return0;
}
